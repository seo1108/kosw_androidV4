<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Kosw.NoActionbar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:buttonStyle">@style/TextViewFontStyle</item>
        <item name="android:editTextStyle">@style/EditTextFontStyle</item>
        <item name="android:radioButtonStyle">@style/TextViewFontStyle</item>
        <item name="android:checkboxStyle">@style/TextViewFontStyle</item>
        <item name="android:textViewStyle">@style/TextViewFontStyle</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="Kosw.FullScreen" parent="Kosw.NoActionbar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="custom_dialog_style" parent="Kosw.NoActionbar">
        <item name="colorPrimary">@color/logo_bg</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="custom_dialog_style_1" parent="Kosw.NoActionbar">
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="CircularProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- font styles -->
    <style name="TextViewFontStyle" parent="@android:style/Widget.DeviceDefault.TextView">
        <item name="android:fontFamily">@font/spoqa</item>
    </style>

    <style name="EditTextFontStyle" parent="@android:style/Widget.DeviceDefault.EditText">
        <item name="android:fontFamily">@font/spoqa</item>
    </style>
</resources>
